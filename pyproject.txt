[project]
name = "hanrei"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "YuTo_Univ", email = "yuuto140107k@gmail.com" }
]
dependencies = [
    "streamlit>=1.39.0",
    "langchain>=0.3.2",
    "langchain-openai>=0.2.1",
    "langchain-qdrant>=0.1.4",
    "qdrant-client>=1.11.3",
    "streamlit-navigation-bar>=3.3.0",
]
readme = "README.md"
requires-python = ">= 3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.rye.scripts]
01 = { cmd = "streamlit run ./src/hanrei/main.py", env = { OPENAI_API_KEY = "" } }
up = { cmd = "python ./src/hanrei/database.py", env = { OPENAI_API_KEY = "", "QDRANT_API_KEY" = "" } }
ask_llm = { cmd = "python ./src/hanrei/ask_llm.py", env = { OPENAI_API_KEY = "", "QDRANT_API_KEY" = "" } }

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/hanrei"]
